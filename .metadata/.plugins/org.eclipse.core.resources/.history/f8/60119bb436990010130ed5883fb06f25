'''
Created on Jun 18, 2011

@author: konstantin
'''
import os

def loader(path):
    configs = os.listdir(path)
    for c in configs:
        if c.endswith('.conf'):
            config = ConfigParser()
            config.read(self.CONFIG_FILE)