'''
Created on Jun 18, 2011

@author: konstantin
'''
import os
from ConfigParser import ConfigParser

config_storage = []

def loader(path):
    confs = os.listdir(path)
    for c in confs:
        if c.endswith('.conf'):
            print ("Loading %s" % s)
            config = ConfigParser()
            config.read(c)
            config_storage.append(config)